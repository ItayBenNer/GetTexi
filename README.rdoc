== README

Assigment v1.0
Date: 28.11.2013
Created by: Itay BenNer

*1. created 'Metric' scaffold

*2. Inside /lib/tasks/ folder creat a new rake file name 'insert_matrics'
	with two tasks

	- metrics:clearall => clear all metric recordes
	- metrics:import['log.json.file.path']

*3. Created new view named locations (for e.g http://localhost:3000/metrics/locations)
	there you can search for metrics by name and see the, on the map as markers with all metric data as overllay.


# Github steps to first commit
	* git add .
	* git add -u
	* git commit -m "First commit"
	* git push origin master